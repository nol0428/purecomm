<!DOCTYPE html>
<html>
  <head>
    <title>PureComm</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags if respond_to?(:javascript_importmap_tags) %>
    <%= stylesheet_link_tag "https://fonts.googleapis.com/css2?family=Raleway:wght@400;600&display=swap", rel: "stylesheet" %>
    <%= action_cable_meta_tag %>
    <link rel="manifest" href="/manifest.webmanifest" crossorigin="use-credentials">
    <meta name="theme-color" content="#6ED3CF">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <link rel="apple-touch-icon" href="/icons/pwa-192.png">
  </head>
  <body class="app-shell has-bottom-nav">
    <a class="skip-link" href="#content">Skip to content</a>
    <% unless controller_name == "pages" && action_name == "home" %>
      <%= render "shared/top_navbar" %>
    <% end %>
    <%= render "shared/flashes" %>
    <main id="content" class="site-main <%= "no-padding" if controller_name == "pages" && action_name == "home" %>">
      <div class="app-container flow">
        <%= yield %>
      </div>
    </main>
    <% unless controller_name == "pages" && action_name == "home" %>
      <%= render "shared/bottom_navbar" %>
    <% end %>
    <%# <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %> %>
    <script>
      document.addEventListener("turbo:after-stream-render", (e) => {
        if (document.getElementById('partner-checkins')) {
          try { window.sortPartnerCheckins && window.sortPartnerCheckins(); } catch(_) {}
        }
        if (document.getElementById('grievance_list')) {
          try { window.sortGrievances && window.sortGrievances(); } catch(_) {}
        }
      });
    </script>
  </body>
</html>

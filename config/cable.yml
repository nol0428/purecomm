development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("CABLE_REDIS_URL") { ENV.fetch("REDIS_URL") } %>
  channel_prefix: purecomm_production
  ssl_params:
    verify_mode: 0   # OpenSSL::SSL::VERIFY_NONE
